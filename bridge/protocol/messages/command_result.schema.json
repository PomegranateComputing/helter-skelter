{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "messages/command_result.schema.json",
  "title": "command.result payload",
  "description": "correlation_id (the id of the originating command.request) is mandatory at the envelope level for kind=command.result -- see envelope.schema.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["engine_cost", "engine_error"],
  "properties": {
    "engine_cost": {
      "description": "Cost reported by the OpenRCT2 game-action system for executing the command; null if the command failed before a cost was determined.",
      "type": ["integer", "null"]
    },
    "engine_error": {
      "anyOf": [
        { "type": "null" },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "message"],
          "properties": {
            "code": { "type": "string" },
            "message": { "type": "string" }
          }
        }
      ]
    }
  }
}
